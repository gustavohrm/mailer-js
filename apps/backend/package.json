{
  "name": "@mailer/backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "pretest": "npm run build",
    "test": "node --test --test-isolation=none test/app.test.mjs",
    "prestart": "npm run build",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "fastify": "^5.6.1",
    "nodemailer": "^7.0.3"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "@types/nodemailer": "^6.4.17",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
